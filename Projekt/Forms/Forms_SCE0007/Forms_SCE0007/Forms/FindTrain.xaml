<UserControl x:Class="Forms_SCE0007.Forms.FindTrain"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Forms_SCE0007.Forms"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<DataGrid Name="dataGrid" ItemsSource="{Binding}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,33,0,0" >
			<DataGrid.Columns>
				<DataGridTextColumn Header="Název"  Binding="{Binding Nazev}"/>
				<DataGridTextColumn Header="Kč/km"  Binding="{Binding CenaZaKm}"/>
				<DataGridTextColumn Header="Kapacita"  Binding="{Binding KapacitaMist}"/>
				<DataGridTextColumn Header="Společnost"  Binding="{Binding Spolecnost.Nazev}"/>
				<DataGridTextColumn Header="Pravidelný"  Binding="{Binding Pravidelny}"/>
			</DataGrid.Columns>
		</DataGrid>
		<TextBox x:Name="tb_input" HorizontalAlignment="Left" Margin="100,4,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120"/>
		<Label Content="Název stanice:" HorizontalAlignment="Left" Margin="10,0,0,0" VerticalAlignment="Top"/>
		<Button Content="Vyhledat" HorizontalAlignment="Left" Margin="225,3,0,0" VerticalAlignment="Top" Width="80" Click="ButtonFind_Click"/>

	</Grid>
</UserControl>
